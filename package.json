{
  "name": "grunt-nose",
  "version": "0.1.1",
  "description": "Run python unit tests using nose",
  "scripts": {
    "devsetup": "pip install nose --target tasks/bin && grunt clean"
  },
  "devDependencies": {
    "grunt": "~0.4.1",
    "grunt-contrib-jshint": "~0.5.4",
    "grunt-contrib-clean": "~0.5.0",
    "matchdep": "~0.3.0",
    "grunt-release": "~0.6.0"
  }
}
